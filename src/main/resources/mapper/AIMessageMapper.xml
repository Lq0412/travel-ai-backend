<?xml version="1.0" encoding="UTF-8"?>
<!--
  File: .\src\main\resources\mapper\AIMessageMapper.xml
  Description: Auto-generated file header comment.
  Created: 2025-10-17
-->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lq.common.AI.core.mapper.AIMessageMapper">

    <!-- 获取对话的消息列表（按时间排序） -->
    <select id="selectConversationMessages" resultType="com.lq.common.AI.core.model.entity.AIMessage">
        SELECT id, conversation_id, role, content, tokens_used, response_time, create_time, is_delete
        FROM ai_message
        WHERE conversation_id = #{conversationId} AND is_delete = 0
        ORDER BY create_time ASC
    </select>

    <!-- 获取对话的最新N条消息 -->
    <select id="selectRecentMessages" resultType="com.lq.common.AI.core.model.entity.AIMessage">
        SELECT id, conversation_id, role, content, tokens_used, response_time, create_time, is_delete
        FROM ai_message
        WHERE conversation_id = #{conversationId} AND is_delete = 0
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>

    <!-- 删除对话的所有消息 -->
    <update id="deleteConversationMessages">
        UPDATE ai_message
        SET is_delete = 1
        WHERE conversation_id = #{conversationId}
    </update>

</mapper>
